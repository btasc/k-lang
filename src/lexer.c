#include "vector.h"
#include "tokens.h"

typedef struct {
    vector_t tokens;
} lexer_data_t;

lexer_data_t make_lexer_data(char *file_text, size_t file_text_len) {
    lexer_data_t data;

    return data;
}

bool check_identifier(char *text) {


    return false;
}